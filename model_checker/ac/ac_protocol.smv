-- ISO 15118-20 AC Charging Protocol Model
-- Generated by AC Protocol Model Checker v2
-- States: 13, Transitions: 32, Variables: 10

MODULE main

VAR
  -- Turn-based execution
  turn : {evcc_turn, secc_turn};
  
  -- Protocol state (shared)
  proto_state : {SupportedAppProtocol, SessionSetup, AuthorizationSetup, Authorization, CertificateInstallation, ServiceDiscovery, ServiceDetail, ServiceSelection, AC_ChargeParameterDiscovery, ScheduleExchange, PowerDelivery, AC_ChargeLoop, SessionStop};
  
  -- EVCC control state
  evcc_ctrl : {EVCC_Idle, EVCC_Send_SupportedAppProtocolReq, EVCC_Wait_SupportedAppProtocolRes, EVCC_Send_SessionSetupReq, EVCC_Wait_SessionSetupRes, EVCC_Send_AuthorizationSetupReq, EVCC_Wait_AuthorizationSetupRes, EVCC_Send_AuthorizationReq, EVCC_Wait_AuthorizationRes, EVCC_Send_CertificateInstallationReq, EVCC_Wait_CertificateInstallationRes, EVCC_Send_ServiceDiscoveryReq, EVCC_Wait_ServiceDiscoveryRes, EVCC_Send_ServiceDetailReq, EVCC_Wait_ServiceDetailRes, EVCC_Send_ServiceSelectionReq, EVCC_Wait_ServiceSelectionRes, EVCC_Send_AC_ChargeParameterDiscoveryReq, EVCC_Wait_AC_ChargeParameterDiscoveryRes, EVCC_Send_ScheduleExchangeReq, EVCC_Wait_ScheduleExchangeRes, EVCC_Send_PowerDeliveryReq, EVCC_Wait_PowerDeliveryRes, EVCC_Send_AC_ChargeLoopReq, EVCC_Wait_AC_ChargeLoopRes, EVCC_Send_SessionStopReq, EVCC_Wait_SessionStopRes};
  
  -- SECC control state
  secc_ctrl : {SECC_Idle, SECC_Wait_SupportedAppProtocolReq, SECC_Process_SupportedAppProtocol, SECC_Send_SupportedAppProtocolRes, SECC_Wait_SessionSetupReq, SECC_Process_SessionSetup, SECC_Send_SessionSetupRes, SECC_Wait_AuthorizationSetupReq, SECC_Process_AuthorizationSetup, SECC_Send_AuthorizationSetupRes, SECC_Wait_AuthorizationReq, SECC_Process_Authorization, SECC_Send_AuthorizationRes, SECC_Wait_CertificateInstallationReq, SECC_Process_CertificateInstallation, SECC_Send_CertificateInstallationRes, SECC_Wait_ServiceDiscoveryReq, SECC_Process_ServiceDiscovery, SECC_Send_ServiceDiscoveryRes, SECC_Wait_ServiceDetailReq, SECC_Process_ServiceDetail, SECC_Send_ServiceDetailRes, SECC_Wait_ServiceSelectionReq, SECC_Process_ServiceSelection, SECC_Send_ServiceSelectionRes, SECC_Wait_AC_ChargeParameterDiscoveryReq, SECC_Process_AC_ChargeParameterDiscovery, SECC_Send_AC_ChargeParameterDiscoveryRes, SECC_Wait_ScheduleExchangeReq, SECC_Process_ScheduleExchange, SECC_Other};
  
  -- Message channel
  msg_channel : {none, SupportedAppProtocolReq, SupportedAppProtocolRes, SessionSetupReq, SessionSetupRes, AuthorizationSetupReq, AuthorizationSetupRes, AuthorizationReq, AuthorizationRes, CertificateInstallationReq, CertificateInstallationRes, ServiceDiscoveryReq, ServiceDiscoveryRes, ServiceDetailReq, ServiceDetailRes, ServiceSelectionReq, ServiceSelectionRes, AC_ChargeParameterDiscoveryReq, AC_ChargeParameterDiscoveryRes, ScheduleExchangeReq, ScheduleExchangeRes, PowerDeliveryReq, PowerDeliveryRes, AC_ChargeLoopReq, AC_ChargeLoopRes, SessionStopReq, SessionStopRes};
  
  -- Protocol variables (ISO names preserved)
  ResponseCode : {FAILED, FAILED_EVPowerProfileViolation, FAILED_SequenceError, FAILED_ServiceIDInvalid, FAILED_ServiceSelectionInvalid, FAILED_WrongChargeParameter, Failed_NoNegotiation, OK, OK_NewSessionEstablished, OK_OldSessionJoined, OK_SuccessfulNegotiation, OK_SuccessfulNegotiationWithMinorDeviation, WARNING, WARNING_AuthorizationSelectionInvalid, WARNING_CertificateValidationError, WARNING_EIMAuthorizationFailure, WARNING_EVPowerProfileViolation, WARNING_GeneralPnCAuthorizationError, WARNING_PowerToleranceNotConfirmed, WARNING_StandbyNotAllowed};  -- Response status from SECC
  EVSEProcessing : {Finished, Ongoing};  -- Whether SECC is still processing
  EVProcessing : {Ongoing, Finished};  -- Whether EVCC is still processing
  ChargeProgress : {Renegotiate, ScheduleRenegotiate, Standby, Start, Stop};  -- EVCC charge progress intention
  ChargingSession : {Pause, ServiceRenegotiation, Terminate};  -- EVCC session termination mode
  EVSENotification : {MeteringConfirmation, Pause, ScheduleRenegotiation, ServiceRenegotiation, Terminate, none};  -- SECC notification to EVCC
  CertificateInstallationService : {e_False, e_True, boolean};  -- Whether certificate installation is available
  ServiceRenegotiationSupported : {e_False, e_True, boolean};  -- Whether service renegotiation is supported
  CertificateInstallIntent : boolean;  -- Whether EVCC wants certificate installation
  ServiceName : {AC, DC, WPT, ACDP, none};  -- Selected energy transfer service name
  
  -- Retry counter
  retry_count : 0..3;
  
  -- Timer (abstracted)
  timer_tick : 0..5;
  timer_expired : boolean;
  
ASSIGN
  init(turn) := evcc_turn;
  init(proto_state) := SupportedAppProtocol;
  init(msg_channel) := none;
  init(ResponseCode) := OK;
  init(EVSEProcessing) := Finished;
  init(EVProcessing) := Finished;
  init(ChargeProgress) := Start;
  init(ChargingSession) := Terminate;
  init(EVSENotification) := none;
  init(CertificateInstallationService) := e_FALSE;
  init(ServiceRenegotiationSupported) := e_FALSE;
  init(CertificateInstallIntent) := FALSE;
  init(ServiceName) := AC;
  init(retry_count) := 0;
  init(timer_tick) := 0;
  init(timer_expired) := FALSE;
  init(evcc_ctrl) := EVCC_Idle;
  init(secc_ctrl) := SECC_Idle;
  
  -- Turn alternation
  next(turn) := case
    turn = evcc_turn : secc_turn;
    turn = secc_turn : evcc_turn;
    TRUE : turn;
  esac;
  
  -- Protocol state transitions
  next(proto_state) := case
    -- T1: SupportedAppProtocol -> SessionSetup
    proto_state = SupportedAppProtocol & (ResponseCode = OK_SuccessfulNegotiation) : SessionSetup;
    -- T2: SessionSetup -> AuthorizationSetup
    proto_state = SessionSetup & (ResponseCode = OK) : AuthorizationSetup;
    -- T3: SessionSetup -> AC_ChargeParameterDiscovery
    proto_state = SessionSetup & (ResponseCode = OK) & (turn = evcc_turn) : AC_ChargeParameterDiscovery;
    -- T4: AuthorizationSetup -> Authorization
    proto_state = AuthorizationSetup & (ResponseCode = OK) : Authorization;
    -- T5: AuthorizationSetup -> CertificateInstallation
    proto_state = AuthorizationSetup & (ResponseCode = OK) : CertificateInstallation;
    -- T26: AuthorizationSetup -> SessionStop
    proto_state = AuthorizationSetup & (ResponseCode = OK) : SessionStop;
    -- T8: Authorization -> CertificateInstallation
    proto_state = Authorization & (ResponseCode = OK) & (EVSEProcessing = Finished) : CertificateInstallation;
    -- T9: Authorization -> Authorization
    proto_state = Authorization & (ResponseCode = OK) & (EVSEProcessing = Finished) : Authorization;
    -- T10: Authorization -> ServiceDiscovery
    proto_state = Authorization & (ResponseCode = OK) & (EVSEProcessing = Finished) : ServiceDiscovery;
    -- T25: Authorization -> SessionStop
    proto_state = Authorization & (ResponseCode = OK) & (EVSEProcessing = Finished) : SessionStop;
    -- T6: CertificateInstallation -> CertificateInstallation
    proto_state = CertificateInstallation & ((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK) : CertificateInstallation;
    -- T7: CertificateInstallation -> Authorization
    proto_state = CertificateInstallation & ((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK) : Authorization;
    -- T27: CertificateInstallation -> SessionStop
    proto_state = CertificateInstallation & ((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK) : SessionStop;
    -- T11: ServiceDiscovery -> SessionStop
    proto_state = ServiceDiscovery & (ServiceRenegotiationSupported = TRUE) : SessionStop;
    -- T12: ServiceDiscovery -> ServiceDetail
    proto_state = ServiceDiscovery & (ServiceRenegotiationSupported = TRUE) : ServiceDetail;
    -- T28: ServiceDiscovery -> SessionStop
    proto_state = ServiceDiscovery & (ServiceRenegotiationSupported = TRUE) : SessionStop;
    -- T13: ServiceDetail -> ServiceDetail
    proto_state = ServiceDetail & (ResponseCode = OK) : ServiceDetail;
    -- T14: ServiceDetail -> ServiceSelection
    proto_state = ServiceDetail & (ResponseCode = OK) : ServiceSelection;
    -- T29: ServiceDetail -> SessionStop
    proto_state = ServiceDetail & (ResponseCode = OK) : SessionStop;
    -- T15: ServiceSelection -> AC_ChargeParameterDiscovery
    proto_state = ServiceSelection & (ResponseCode = OK) : AC_ChargeParameterDiscovery;
    -- T30: ServiceSelection -> SessionStop
    proto_state = ServiceSelection & (ResponseCode = OK) : SessionStop;
    -- T16: AC_ChargeParameterDiscovery -> ScheduleExchange
    proto_state = AC_ChargeParameterDiscovery : ScheduleExchange;
    -- T31: AC_ChargeParameterDiscovery -> SessionStop
    proto_state = AC_ChargeParameterDiscovery : SessionStop;
    -- T17: ScheduleExchange -> ScheduleExchange
    proto_state = ScheduleExchange & (EVSEProcessing = Ongoing) : ScheduleExchange;
    -- T18: ScheduleExchange -> PowerDelivery
    proto_state = ScheduleExchange & (EVSEProcessing = Ongoing) : PowerDelivery;
    -- T32: ScheduleExchange -> SessionStop
    proto_state = ScheduleExchange & (EVSEProcessing = Ongoing) : SessionStop;
    -- T19: PowerDelivery -> PowerDelivery
    proto_state = PowerDelivery & (EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) : PowerDelivery;
    -- T20: PowerDelivery -> AC_ChargeLoop
    proto_state = PowerDelivery & (EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) : AC_ChargeLoop;
    -- T23: PowerDelivery -> SessionStop
    proto_state = PowerDelivery & (EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) & (turn = secc_turn) : SessionStop;
    -- T21: AC_ChargeLoop -> PowerDelivery
    proto_state = AC_ChargeLoop : PowerDelivery;
    -- T22: AC_ChargeLoop -> AC_ChargeLoop
    proto_state = AC_ChargeLoop : AC_ChargeLoop;
    -- T24: SessionStop -> ServiceDiscovery
    proto_state = SessionStop & (ResponseCode = OK) & (EVSENotification = Terminate) & (ServiceRenegotiationSupported = TRUE) : ServiceDiscovery;
    TRUE : proto_state;
  esac;
  
  -- Message channel follows protocol state
  next(msg_channel) := case
    next(proto_state) = SupportedAppProtocol & turn = evcc_turn : SupportedAppProtocolReq;
    next(proto_state) = SupportedAppProtocol & turn = secc_turn : SupportedAppProtocolRes;
    next(proto_state) = SessionSetup & turn = evcc_turn : SessionSetupReq;
    next(proto_state) = SessionSetup & turn = secc_turn : SessionSetupRes;
    next(proto_state) = AuthorizationSetup & turn = evcc_turn : AuthorizationSetupReq;
    next(proto_state) = AuthorizationSetup & turn = secc_turn : AuthorizationSetupRes;
    next(proto_state) = Authorization & turn = evcc_turn : AuthorizationReq;
    next(proto_state) = Authorization & turn = secc_turn : AuthorizationRes;
    next(proto_state) = CertificateInstallation & turn = evcc_turn : CertificateInstallationReq;
    next(proto_state) = CertificateInstallation & turn = secc_turn : CertificateInstallationRes;
    next(proto_state) = ServiceDiscovery & turn = evcc_turn : ServiceDiscoveryReq;
    next(proto_state) = ServiceDiscovery & turn = secc_turn : ServiceDiscoveryRes;
    next(proto_state) = ServiceDetail & turn = evcc_turn : ServiceDetailReq;
    next(proto_state) = ServiceDetail & turn = secc_turn : ServiceDetailRes;
    next(proto_state) = ServiceSelection & turn = evcc_turn : ServiceSelectionReq;
    next(proto_state) = ServiceSelection & turn = secc_turn : ServiceSelectionRes;
    next(proto_state) = AC_ChargeParameterDiscovery & turn = evcc_turn : AC_ChargeParameterDiscoveryReq;
    next(proto_state) = AC_ChargeParameterDiscovery & turn = secc_turn : AC_ChargeParameterDiscoveryRes;
    next(proto_state) = ScheduleExchange & turn = evcc_turn : ScheduleExchangeReq;
    next(proto_state) = ScheduleExchange & turn = secc_turn : ScheduleExchangeRes;
    next(proto_state) = PowerDelivery & turn = evcc_turn : PowerDeliveryReq;
    next(proto_state) = PowerDelivery & turn = secc_turn : PowerDeliveryRes;
    next(proto_state) = AC_ChargeLoop & turn = evcc_turn : AC_ChargeLoopReq;
    next(proto_state) = AC_ChargeLoop & turn = secc_turn : AC_ChargeLoopRes;
    next(proto_state) = SessionStop & turn = evcc_turn : SessionStopReq;
    next(proto_state) = SessionStop & turn = secc_turn : SessionStopRes;
    TRUE : none;
  esac;
  
  -- ResponseCode: updated by actions
  next(ResponseCode) := case
    proto_state = CertificateInstallation : per;  -- T6
    proto_state = CertificateInstallation : per;  -- T7
    proto_state = CertificateInstallation : per;  -- T7
    proto_state = Authorization : WARNING_GeneralPnCAuthorizationError;  -- T8
    proto_state = Authorization : WARNING_EIMAuthorizationFailure;  -- T8
    proto_state = Authorization : WARNING_GeneralPnCAuthorizationError;  -- T9
    proto_state = Authorization : WARNING_EIMAuthorizationFailure;  -- T9
    proto_state = Authorization : WARNING_GeneralPnCAuthorizationError;  -- T10
    proto_state = Authorization : WARNING_EIMAuthorizationFailure;  -- T10
    proto_state = PowerDelivery : FAILED_PowerDeliveryNotApplied;  -- T19
    proto_state = PowerDelivery : OK;  -- T19
    proto_state = PowerDelivery : FAILED_PowerDeliveryNotApplied;  -- T20
    proto_state = PowerDelivery : OK;  -- T20
    proto_state = PowerDelivery : FAILED_PowerDeliveryNotApplied;  -- T23
    proto_state = PowerDelivery : OK;  -- T23
    proto_state = Authorization : WARNING_GeneralPnCAuthorizationError;  -- T25
    proto_state = Authorization : WARNING_EIMAuthorizationFailure;  -- T25
    proto_state = CertificateInstallation : per;  -- T27
    TRUE : ResponseCode;
  esac;
  
  -- EVSEProcessing: per-state constrained
  next(EVSEProcessing) := case
    proto_state = Authorization : {Finished, Ongoing};  -- at Authorization
    proto_state = CertificateInstallation : {Finished, Ongoing};  -- at CertificateInstallation
    proto_state = ScheduleExchange : {Finished, Ongoing};  -- at ScheduleExchange
    TRUE : EVSEProcessing;
  esac;
  
  -- EVProcessing: per-state constrained
  next(EVProcessing) := case
    proto_state = PowerDelivery : {Ongoing};  -- at PowerDelivery
    TRUE : EVProcessing;
  esac;
  
  -- ChargeProgress: per-state constrained
  next(ChargeProgress) := case
    proto_state = PowerDelivery : {Standby, Stop};  -- at PowerDelivery
    TRUE : ChargeProgress;
  esac;
  
  -- ChargingSession: per-state constrained
  next(ChargingSession) := case
    proto_state = SessionStop : {Pause, ServiceRenegotiation, Terminate};  -- at SessionStop
    TRUE : ChargingSession;
  esac;
  
  -- EVSENotification: per-state constrained
  next(EVSENotification) := case
    proto_state = SessionStop : {Terminate};  -- at SessionStop
    TRUE : EVSENotification;
  esac;
  
  -- CertificateInstallationService: no state constraints, keep value
  next(CertificateInstallationService) := CertificateInstallationService;
  
  -- ServiceRenegotiationSupported: updated by actions
  next(ServiceRenegotiationSupported) := case
    proto_state = ServiceDiscovery : TRUE;  -- T11
    proto_state = ServiceDiscovery : TRUE;  -- T12
    proto_state = ServiceDiscovery : TRUE;  -- T28
    TRUE : ServiceRenegotiationSupported;
  esac;
  
  -- CertificateInstallIntent: constrained nondeterministic (boolean)
  next(CertificateInstallIntent) := {CertificateInstallIntent, !CertificateInstallIntent};
  
  -- ServiceName: no state constraints, keep value
  next(ServiceName) := ServiceName;
  
  -- Retry counter
  next(retry_count) := case
    next(proto_state) != proto_state : 0;
    next(proto_state) = proto_state & retry_count < 3 : retry_count + 1;
    TRUE : retry_count;
  esac;
  
  -- Timer abstraction
  next(timer_tick) := case
    next(proto_state) != proto_state : 0;  -- reset on state change
    timer_tick < 5 : timer_tick + 1;
    TRUE : timer_tick;
  esac;
  
  next(timer_expired) := timer_tick >= 5;
  

-- Transition mutual exclusion constraints
-- Mutual exclusion for transitions from SessionSetup
TRANS proto_state = SessionSetup -> !((ResponseCode = OK) & (ResponseCode = OK) & (turn = evcc_turn))
-- Mutual exclusion for transitions from AuthorizationSetup
TRANS proto_state = AuthorizationSetup -> !((ResponseCode = OK) & (ResponseCode = OK))
TRANS proto_state = AuthorizationSetup -> !((ResponseCode = OK) & (ResponseCode = OK))
TRANS proto_state = AuthorizationSetup -> !((ResponseCode = OK) & (ResponseCode = OK))
-- Mutual exclusion for transitions from CertificateInstallation
TRANS proto_state = CertificateInstallation -> !(((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK) & ((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK))
TRANS proto_state = CertificateInstallation -> !(((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK) & ((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK))
TRANS proto_state = CertificateInstallation -> !(((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK) & ((EVSEProcessing = ONGOING | EVSEProcessing = Finished)) & (ResponseCode = OK))
-- Mutual exclusion for transitions from Authorization
TRANS proto_state = Authorization -> !((ResponseCode = OK) & (EVSEProcessing = Finished) & (ResponseCode = OK) & (EVSEProcessing = Finished))
TRANS proto_state = Authorization -> !((ResponseCode = OK) & (EVSEProcessing = Finished) & (ResponseCode = OK) & (EVSEProcessing = Finished))
TRANS proto_state = Authorization -> !((ResponseCode = OK) & (EVSEProcessing = Finished) & (ResponseCode = OK) & (EVSEProcessing = Finished))
TRANS proto_state = Authorization -> !((ResponseCode = OK) & (EVSEProcessing = Finished) & (ResponseCode = OK) & (EVSEProcessing = Finished))
TRANS proto_state = Authorization -> !((ResponseCode = OK) & (EVSEProcessing = Finished) & (ResponseCode = OK) & (EVSEProcessing = Finished))
TRANS proto_state = Authorization -> !((ResponseCode = OK) & (EVSEProcessing = Finished) & (ResponseCode = OK) & (EVSEProcessing = Finished))
-- Mutual exclusion for transitions from ServiceDiscovery
TRANS proto_state = ServiceDiscovery -> !((ServiceRenegotiationSupported = TRUE) & (ServiceRenegotiationSupported = TRUE))
TRANS proto_state = ServiceDiscovery -> !((ServiceRenegotiationSupported = TRUE) & (ServiceRenegotiationSupported = TRUE))
TRANS proto_state = ServiceDiscovery -> !((ServiceRenegotiationSupported = TRUE) & (ServiceRenegotiationSupported = TRUE))
-- Mutual exclusion for transitions from ServiceDetail
TRANS proto_state = ServiceDetail -> !((ResponseCode = OK) & (ResponseCode = OK))
TRANS proto_state = ServiceDetail -> !((ResponseCode = OK) & (ResponseCode = OK))
TRANS proto_state = ServiceDetail -> !((ResponseCode = OK) & (ResponseCode = OK))
-- Mutual exclusion for transitions from ServiceSelection
TRANS proto_state = ServiceSelection -> !((ResponseCode = OK) & (ResponseCode = OK))
-- Mutual exclusion for transitions from AC_ChargeParameterDiscovery
-- Mutual exclusion for transitions from ScheduleExchange
TRANS proto_state = ScheduleExchange -> !((EVSEProcessing = Ongoing) & (EVSEProcessing = Ongoing))
TRANS proto_state = ScheduleExchange -> !((EVSEProcessing = Ongoing) & (EVSEProcessing = Ongoing))
TRANS proto_state = ScheduleExchange -> !((EVSEProcessing = Ongoing) & (EVSEProcessing = Ongoing))
-- Mutual exclusion for transitions from PowerDelivery
TRANS proto_state = PowerDelivery -> !((EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) & (EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)))
TRANS proto_state = PowerDelivery -> !((EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) & (EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) & (turn = secc_turn))
TRANS proto_state = PowerDelivery -> !((EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) & (EVSEProcessing = NOT_APPLICABLE) & (ResponseCode = OK) & (EVProcessing = Ongoing) & ((ChargeProgress = Stop | ChargeProgress = Standby)) & (turn = secc_turn))
-- Mutual exclusion for transitions from AC_ChargeLoop

-- Timer specifications (from Table 215)
-- V2G_EVCC_Msg_Timer: 0.0s (EVCC) for x
-- V2G_SECC_Msg_Timer: 0.0s (SECC)
-- V2G_EVCC_Sequence_Timer: 0.0s (EVCC) for x
-- V2G_SECC_Sequence_Timer: 0.0s (SECC)
-- V2G_EVCC_Ongoing_Timer: 0.0s (EVCC) for x
-- V2G_SECC_Ongoing_Timer: 0.0s (SECC)
-- V2G_EVCC_Msg_Timeout (MessageType): 0.0s (EVCC) for x
-- V2G_SECC_Msg_Performance_Time (MessageType): 0.0s (SECC)
-- V2G_EVCC_Sequence_Performance_Time: 0.25s (EVCC) for x
-- V2G_SECC_Sequence_Timeout: 0.5s (SECC)
-- V2G_EVCC_Ongoing_Timeout: 0.0s (EVCC) for x
-- V2G_SECC_Ongoing_Performance_Time: 0.0s (SECC)
-- V2G_EVCC_Msg_Timeout(MessageType): 2.0s (EVCC) for SupportedAppProtocolReq
-- V2G_SECC_Msg_Performance_Time(MessageType): 1.5s (SECC) for AC_ChargeLoopReq
-- V2G_EVCC_CommunicationSetup_Timer: 0.0s (EVCC)
-- V2G_SECC_CommunicationSetup_Timer: 0.0s (SECC)
-- V2G_EVCC_DC_CableCheck_Timer: 0.0s (EVCC)
-- V2G_SECC_DC_CableCheck_Timer: 0.0s (SECC)
-- V2G_EVCC_DC_PreCharge_Timer: 0.0s (EVCC)
-- V2G_SECC_DC_PreCharge_Timer: 0.0s (SECC)
-- V2G_EVCC_DC_CableCheck_Timeout: 0.0s (EVCC)
-- V2G_SECC_DC_CableCheck_Performance_Time: 0.0s (SECC)
-- V2G_EVCC_DC_PreCharge_Timeout: 0.0s (EVCC)
-- V2G_SECC_DC_PreCharge_Performance_Time: 0.0s (SECC)
-- communication setup timer: 0.0s (EVCC)
-- CPM4PE_Expiry_Timer: 120.0s (EVCC) for CPM4PE_Activation
-- SECC_NewSessionTicket_Update_Timer: 604800.0s (SECC) for NewSessionTicket
-- SECCDiscoveryTimeout: 0.25s (EVCC) for SECCDiscoveryRequest
-- V2G_Session_Inactivity_Timer: 648000.0s (BOTH) for any message
-- V2G_SECC_Msg_Performance_Time: 0.25s (SECC) for SessionSetupRes
-- AckMaxDelay: 0.0s (EVCC) for AC_ChargeLoopRes
-- message_timer: 0.0s (BOTH) for VehicleCheckIn, VehicleCheckOut
-- sequence_timer: 0.0s (BOTH) for VehicleCheckIn, VehicleCheckOut
-- ongoing_timer: 0.0s (BOTH) for repeated_request_response_pairs_with_EVSEProcessing_Ongoing
-- communication_setup_timer: 0.0s (BOTH) for from_data_link_establishment_to_session_setup_message
-- V2G_EVCC_Msg_Timeout: 2.0s (EVCC) for SupportedAppProtocolReq, SessionSetupReq, VehicleCheckInReq
-- V2G_SECC_Msg_Performance_Timer: 3.0s (SECC) for PowerDeliveryReq
-- application_level_timeout: 0.0s (BOTH) for all_request_response_message_sequences
-- V2G_EVCC_Sequence_Performance_Timeout: 0.0s (EVCC) for SessionStopReq
-- V2G_EVCC_SessionStopTimeout: 0.0s (EVCC)
-- V2G_EVCC_AC_CL_Timeout: 0.0s (EVCC)
-- V2G_EVCC_To_SECC_Timeout: 0.0s (EVCC)
-- V2G_EVCC_SendTimeout: 0.0s (EVCC)
-- V2G_EVSE_ResponseTimeout: 0.0s (BOTH)
-- V2G_EVSEMaxTimeToWaitForChargeLoopReq: 0.0s (BOTH)
-- V2G_EVCC_SendChargeLoopReqTimeout: 0.0s (EVCC)
-- V2G_EVCC_ChargeLoopReqTimeout: 0.0s (EVCC)
-- V2G_EVCC_SessionStopReq_Timeout: 0.0s (EVCC)
-- V2G_EVSE_Processing_Timeout: 0.0s (BOTH)
-- V2G_EVCC_PowerDeliveryReq_StartTimeout: 0.0s (EVCC)
-- V2G_EVCC_DCCL_Timeout: 0.0s (EVCC)
-- V2G_EVSEMaxTimeOut: 0.0s (BOTH)
-- V2G_EVCC_To_SECC_LongestCommTimeout: 0.0s (EVCC)
-- V2G_EVCC_SECC_Timeout: 0.0s (EVCC)
-- V2G_EVCC_SendChargeLoopReq_Timeout: 0.0s (EVCC)
-- V2G_SECC_SessionStopTimeout: 0.0s (SECC)
-- V2G_EVCC_Timeout: 0.0s (EVCC)
-- V2G_EVCC_ServiceDetailReq_Timeout: 0.0s (EVCC)
-- V2G_EVCC_SendMessageTimeout: 0.0s (EVCC)
-- V2G_SECC_SVCStatusTimeout: 0.0s (SECC)
-- V2G_EVCC_ServiceDiscoveryTimeout: 0.0s (EVCC)
-- V2G_EVSE_VoltageAdjustmentTimeout: 0.0s (BOTH)
-- V2G_EVCC_SECC_ComMaxTimeWaitForSessionStopRes: 0.0s (EVCC)
-- V2G_EVCC_ACChargeLoopReqTimeout: 0.0s (EVCC)
-- V2G_EVCC_Transaction_Timeout: 0.0s (EVCC)
-- V2G_EVSE_Response_Timeout: 0.0s (BOTH)
-- V2G_EVCC_Send_PaymentServiceSelectionReq_Timeout: 0.0s (EVCC)
-- V2G_SECC_FullHandshakeTimeout: 0.0s (SECC)
-- V2G_EVCC_Pause_Period: 0.0s (EVCC)
-- V2G_Communication_Session: 0.0s (BOTH)

-- Invariants from requirements
INVAR certificateValidationConformance = IETF_RFC_5280  -- V2G20-1001
INVAR WLAN_STA_MAC_Address_Randomization = TRUE  -- V2G20-2253

-- Fairness: both actors get infinite turns
FAIRNESS turn = evcc_turn
FAIRNESS turn = secc_turn

FAIRNESS !(proto_state = SupportedAppProtocol)
FAIRNESS !(proto_state = SessionSetup)
FAIRNESS !(proto_state = AuthorizationSetup)
FAIRNESS !(proto_state = Authorization)
FAIRNESS !(proto_state = CertificateInstallation)
FAIRNESS !(proto_state = ServiceDiscovery)
FAIRNESS !(proto_state = ServiceDetail)
FAIRNESS !(proto_state = ServiceSelection)
FAIRNESS !(proto_state = AC_ChargeParameterDiscovery)
FAIRNESS !(proto_state = ScheduleExchange)
FAIRNESS !(proto_state = PowerDelivery)
FAIRNESS !(proto_state = AC_ChargeLoop)

